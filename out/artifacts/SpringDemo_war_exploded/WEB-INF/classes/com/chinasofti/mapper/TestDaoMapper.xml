<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinasofti.dao.TestDao">
    <insert id="insertTb" parameterType="User">
        insert into user
        (id,name,password)
        value
        (#{id},#{name},#{password})
    </insert>
</mapper>